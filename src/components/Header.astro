---
import heroImage from "../assets/triangle_background.png";

const { data } = Astro.props;
---

<header
  class=`bg-center text-center relative text-white`
  style=`background-image: url(${heroImage.src})`
>
  <slot />
</header>
